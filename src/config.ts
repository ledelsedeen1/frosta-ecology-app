/**
 *  * @license
  * SPDX-License-Identifier: Apache-2.0
   */

   /**
    * Central application configuration.
     * Set demoMode to true to show the demo/test warning banner.
      * Set demoMode to false to hide the banner in production.
       */
       export const appConfig: { demoMode: boolean } = {
         demoMode: true,
         };